# Hema v0.6.0 (v2026.5.30)

This local Hema release packages the current full offline build as version 0.6.

## Included

- Updated Hema/Hermes package metadata to `0.6.0`.
- Kept the bundled hermes-web-ui runtime at `0.5.16`.
- Included the current local skills tree and Windows full-local packaging flow.

## Package

Build with:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts\build-full-local-package.ps1
```
